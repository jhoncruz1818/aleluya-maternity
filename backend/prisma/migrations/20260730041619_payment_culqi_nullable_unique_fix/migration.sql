BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[Payment] DROP CONSTRAINT [Payment_culqiChargeId_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
